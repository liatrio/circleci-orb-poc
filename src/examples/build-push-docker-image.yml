description: >
  Build and push docker image to ECR.

usage:
  version: 2.1
  orbs:
    liatrio-orb: liatrio-poc/circleci-orb-poc@0.0.5
  workflows:
    pipeline_name:
      jobs:
        - custom-orb/build-push-docker-image:
            executor: aws-ecr/default
            repo-name: my-repo
