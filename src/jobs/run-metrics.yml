description: |
  Gather metrics
executor: << parameters.executor >>
parameters:
  executor:
    default: default
    type: executor
    description: The name of a custom executor to use
steps:
  - checkout
  - run-metrics
